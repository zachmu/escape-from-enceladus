using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework.Graphics;

namespace Test {
    class Player {

        public void Draw(SpriteBatch spriteBatch, Camera c) {
            //for ( int y = 0; y < Height; y++ ) {
            //    for ( int x = 0; x < Width; x++ ) {
            //        int index = y * Width + x;
            //        Tiles[index].Draw(spriteBatch, x, y);
            //    }
            //}
        }
    }
}
